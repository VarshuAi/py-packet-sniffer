# Python Raw Packet Sniffer
A raw socket packet sniffer built in Python. Designed for low-overhead packet inspection on Linux and Termux platforms.

## Prerequisites
Must be run on Linux/Termux with root/sudo access:
```bash
sudo python sniffer.py
```